# CRM Smart Features — Master Brainstorm (updated 2026-04-22)

Framework: Apple Smart Shuffle pattern. Familiar primitives stay; quiet intelligence layered on top. Visual scaffolds shipped; most algorithms are heuristics that can swap to Sparky-LLM calls without changing the UI contract.

## What shipped this session (Apr 21–22)

Each row: surface · what it does · shipped commit.

### Primary surfaces

| # | Surface | Smart Layer | Commit |
|---|---|---|---|
| 1 | **Smart List** (QUICK) | Score + rank by urgency; reason chip per row | `a4d03ae` |
| 2 | **Smart Pipeline** | Stale-card red ring · viewed-quote gold ring · column avg-days & STALE count · per-column empty ghost | `5ae9dfe` + `4e20001` |
| 3 | **Smart Messages** | URGENT / STORM / MEDICAL / READY / QUESTION / OFFER chip on inbound thread previews | `5ae9dfe` |
| 4 | **Smart Proposals** | HOT / UNOPENED / STUCK / EXIT chip + priority sort | `5ae9dfe` |
| 5 | **Smart Invoices** | VIEWED · UNPAID / OVERDUE / UNOPENED / STALE chip + priority sort | `5ae9dfe` |
| 6 | **Smart Calls** | VOICEMAIL / MISSED / RETURN chip + callback-first sort | `5ae9dfe` |
| 7 | **Smart Contact Detail** | "Next best action" hint card with CTA routing to right sub-tab | `5ae9dfe` |
| 8 | **Smart Permits** | NO JURISDICTION / SUBMIT NEXT / STALLED chip + priority sort | `5ae9dfe` |
| 9 | **Smart Materials** | INSTALL IN ≤3D / ORDER THIS WEEK chip + install-date sort | `5ae9dfe` |
| 10 | **Smart Quote Modal** | IN RANGE / CUSTOM / HIGH TICKET / BELOW MARGIN hint | `5ae9dfe` |
| 11 | **Smart New Lead** | LOCAL vs OUT-OF-MARKET area-code chip | `5ae9dfe` |
| 12 | **Smart Calendar** | Day heat bar (green/gold/red density) + OVERLAP chip + red ring when installs are <2h apart | `5ae9dfe` |
| 13 | **Smart Briefing** | "Sparky noticed…" one-line insight row at top | `5ae9dfe` |
| 14 | **Smart Install Brief** | Blocker strip (missing address/amp/panel/permit#) + GO button to Maps | `a1d4b09` + `9f3dc78` |
| 15 | **Smart Stage Picker** | Gold-ring NEXT chip on the suggested next stage | `a1d4b09` |
| 16 | **Smart Notes** | Regex auto-extract facts (amp, panel brand, kW, service, sqft) chip row | `a1d4b09` |
| 17 | **Smart Snooze** | Stage-aware suggested preset (1d new / 3d quoted / 7d booked+) with gold ring | `a1d4b09` |
| 18 | **Smart Auto-Extract Contact Info** | Regex-scan every inbound SMS for email + address; auto-patch contact row if empty | `1fa9032` |
| 19 | **Smart Quick Replies** | Tap-to-fill chips above compose bar driven off latest inbound keywords | `ea7353b` |
| 20 | **Smart Search** (⌘K) | Natural-language intent parser ("who owes me money", "installs today", "near greenville") + SMART · `<intent>` chip on results | `f66e1c0` |
| 21 | **Smart Empty States** | Every empty view pairs a pixel label with a plain-English "what to do about it" hint | `4e20001` |

### Shared tokens (tokens.css)

- `.smart-chip` (6 tones: gold · red · green · navy · purple · muted)
- `.smart-ring-*` · `.smart-bar--*` · `.smart-dot--*`
- `.smart-hint` · `.smart-hint__label` · `.smart-hint__body` · `.smart-hint__cta`

## Backlog — next sessions

Shipped 2026-04-22:

- **Smart Calendar gap-suggest** ✓ — empty future days in the week grid surface a dashed SUGGEST ghost pulled from the AWAITING DATE queue (cycled by day index); click → book 10am that day. `ff207f8`
- **Smart Sparky insights (real LLM)** ✓ — briefing calls `ai-taskmaster` (mode: briefing) once per day (session-cached); contact detail calls (mode: contact_insight) cached per-contact for 15 min. Both fall back to the prior heuristic on failure. `ff207f8`
- **Smart Voice-call transcription inline** ✓ — Calls tab now pulls `['call', 'voicemail', 'missed']`, parses the "Voicemail from X: <transcript>" body twilio-voice already writes, and renders inline `<audio>` + transcript readout per row. `ff207f8`
- **Smart Today widget** ✓ — dismissible cold-open summary above the Smart List (new leads / approved / paid / installs today). `ff207f8`

Still valuable, not yet shipped:

- **Smart Install Brief ETA** — show driving time from Key's current location (or Greenville origin) to the install address. Requires geocoding.
- **Smart Contact Dedup surfacing** — show "X lives at this address already" when creating a new lead.
- **Smart Storm-urgency banner** — poll NWS for severe weather alerts in SC counties; when active, prepend a storm-urgency line to outgoing Alex templates.
- **Smart Price Suggestion** — replace the static $1,197–$1,497 QuickQuote hint with a real percentile from prior proposals of the same amp + cord length.
- **Smart Invoice payment nudge drip** — auto-suggest sending a reminder at day 2, day 4, day 7 for unpaid invoices; one-click to send.
- **Smart Materials template** — pre-check parts when the quote's amp + cord length are known.

## Rules for future smart work

1. **Keep the base familiar.** Don't replace a list with a novel UI just to add AI. Layer underneath.
2. **No visible AI chrome.** No robot emoji, no "powered by Sparky" — the intelligence should feel like the list just knows.
3. **Scaffold first, algorithm second.** Land the visual before wiring the real model so the UI contract is stable.
4. **One chip per row max.** Multiple stacked badges defeat the at-a-glance principle.
5. **Tone palette.** gold = peak interest. red = overdue/stuck/urgent. green = on track/paid. navy = info. purple = permit/process. muted = default/low-priority.
6. **Regex > LLM > nothing.** Start with a cheap regex heuristic. Upgrade to an LLM pass only when the heuristic clearly fails a real case.
